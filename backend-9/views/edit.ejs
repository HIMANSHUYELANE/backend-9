
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-slate-900">
    <div>
        <a class="font-semibold text-zinc-400" href="/read">All Users</a>
    </div>
    <h1 class=" text-cyan-500 my-2 border-b-2 text-center text-2xl font-bold">Create user</h1>
    <form action="/update/<%= user._id %>" method="post" class="text-white flex flex-col w-[60%] space-y-4 p-7 mx-auto">
        <input class="p-1 bg-transparent border-2 rounded-lg outline-none border-gray-500" value="<%= user.name %>" name="name" placeholder="name" type="text">
        <input class="p-1 bg-transparent border-2 rounded-lg outline-none border-gray-500" value="<%= user.email %>" name="email" placeholder="email" type="text">
        <input class="p-1 bg-transparent border-2 rounded-lg outline-none border-gray-500" value="<%= user.image %>" name="image" placeholder="url" type="text">
        <input class="border-2 rounded-lg border-cyan-500 hover:bg-orange-500" type="submit" value="Update User">
    </form>
</body>
</html>